{
  "name": "loadspot-table",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --config config/vite.config.js --host 127.0.0.1",
    "build": "vite build --config config/vite.config.js",
    "preview": "vite preview --config config/vite.config.js --host 127.0.0.1"
  },
  "engines": {
    "node": ">=20.19.0"
  },
  "devDependencies": {
    "vite": "^8.0.10"
  }
}
